# Methods

# We invoke functions using their name, like len:
length = len("hej!")

# We've also used other, special functions, which are attached to
# objects, like append(..)

fruits = ["banana", "apple", "orange"]
fruits.append("mango")

# These are called 'methods' -- they execute in the context of the
# object.

# We cannot call these functions without an object - this gives an
# error:
# append("mango")


